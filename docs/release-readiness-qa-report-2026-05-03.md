# FANZONE Release Readiness QA Report

Date: 2026-05-03
Scope: Flutter mobile client, venue web PWA, admin web PWA, TV display PWA, Supabase migrations, Edge Functions, RLS/security posture, wallet/ledger, ordering, pools, dashboard, realtime, and TV/game requirements. `apps/website` is a separate public web PWA surface and must not be treated as the Flutter mobile client.

## 1. Executive Summary

Release recommendation: Not ready.

The implemented product has a working simplified sports-bar foundation: WhatsApp OTP tests exist, no-name mobile onboarding is present, venue browsing/ordering is implemented, off-platform payment handoff exists, staff manual payment confirmation is audited, user FET wallets use a ledgered mutation function, and basic pool staking/settlement flows exist.

Remediation update: the critical server-side product-rule gaps found in the first audit pass now have targeted Supabase hardening migrations and client/dashboard contract updates. The remote FANZONE Supabase project has migrations through `20260503140000_targeted_performance_policy_initplans.sql` applied. These add immutable unique fan IDs, fan-profile category limits, venue FET wallets and ledgering, venue-only pool creation, join-deadline enforcement, order-linked eligibility settlement, venue staff settlement wrapper, centralized game templates/sessions/questions, 20-question stable selection, first-correct answer protection, Music Bingo claims, venue-isolated TV screen state, hardened grants, security-advisor error remediation, and targeted performance WARN cleanup for release-touched RLS policies.

The app is still not signed off for production because production credential rotation remains mandatory after credential exposure in chat, authenticated venue-staff UAT is blocked by missing `venue_users`/staff test fixtures on the linked database, mobile UAT must run on simulator/device rather than Flutter web, and the dashboard/TV game surfaces still need real authenticated RPC/realtime UAT beyond route screenshots. The standalone TV display PWA now exists at `apps/tv-display` and reads `venue_screen_states`, but it still needs live multi-surface UAT with seeded venue data before release signoff. Flutter, TypeScript, npm, remote SQL contract tests, remote RLS tests, route screenshots, and Supabase advisor ERROR checks pass in the current worktree/project.

## 2. Critical Product-Rule Compliance

| Rule area | Status | Evidence / notes |
| --- | --- | --- |
| WhatsApp OTP login, no name required | Partial pass | OTP function/tests exist. Onboarding does not require a name. User profile columns still include optional name fields, which is acceptable only if never required. |
| Autogenerated unique 6-digit ID | Fixed on remote DB | New trigger preserves existing `fan_id` on update and allocates new IDs under advisory lock. |
| Fan profile category limits | Fixed pending live UAT | Onboarding and profile editing now support local/top-European/national selections. DB trigger enforces 1 local, 2 top European, and 2 national teams while preserving legacy source compatibility. |
| Venue browsing/menu/order | Partial pass | Core ordering exists and validates venue/table/items server-side. Full UAT with live Supabase was not executed. |
| Payment status and manual confirmation | Partial pass | `payment_submitted`, `preparing`, amount, method, and reference support now exist in current workspace. Payment confirmation is staff-only and audited. |
| FET user wallet ledger | Fixed on remote DB, pending UAT | User wallet ledger already exists; new migration adds venue wallets and venue wallet transactions with restricted mutation RPCs. Platform wallet remains out of MVP unless separately required. |
| Prediction pools | Fixed on remote DB, pending UAT | New migration requires venue-linked pools, blocks locked/live joins, reserves bar stakes, and settles with eligibility filtering. |
| Entertainment games | Backend fixed, dashboard UAT partial | New migration adds centralized templates, venue sessions, teams, question selection, scoring, Music Bingo claims, and TV state. Unauthenticated route screenshots render; authenticated RPC/realtime UAT still needs staff fixtures. |
| 20-question session selection | Fixed on remote DB, pending UAT | `create_game_session` stores exactly 20 active approved questions for trivia/song-guess templates. |
| First-correct-team validation | Fixed on remote DB, pending UAT | `submit_game_answer` has one answer per team/question and a partial unique index for the first correct team per question. |
| Music Bingo / Song Guess | Backend fixed pending UI/UAT | Music Bingo card/claim/verification primitives are implemented without music streaming. |
| 2-hour qualifying-order eligibility | Fixed on remote DB, pending UAT | `user_has_qualifying_order` and settlement enforce paid, non-cancelled orders from the linked venue in the inclusive 2-hour pre-start window. |
| Global viewing | Fixed on remote DB, pending UAT | Creation RPCs now require venue scope/venue ID; global/country remain browsing/filter concepts. |
| Venue dashboard | Partial | Pool creation now sends bar stake. Live unauthenticated route screenshots were captured for overview/orders/pools/games/wallet/screen; authenticated staff workflows need test staff fixtures. |
| TV/live screen | Partial | Venue-isolated screen state RPC/table exists, `/screen` control/preview route renders, and `apps/tv-display` provides the standalone paired display PWA. Realtime mode switching still needs authenticated UAT with venue fixtures. |
| Realtime | Partial | Venue orders/stats/pools and Flutter order tracking subscribe to Supabase changes. Game/TV realtime display behavior still needs end-to-end UAT. |
| Security/RLS | Partial | Remote RLS tests pass and Supabase advisor ERROR checks pass. Credential rotation remains required. |
| Data integrity | Partial | Remote SQL tests cover wallet/settlement/game invariants; production-scale reconciliation and live UAT remain required. |

## 3. Defect List

### FZ-QA-001 - Settlement pays winners without qualifying-order eligibility

- Severity: Blocker
- Area: Wallet / Pools / Settlement
- Steps: Create or join a pool with no paid order from the linked venue in the 2-hour pre-start window, choose the winning camp, settle the pool.
- Expected: Winner is marked `Won - Ineligible / No qualifying order`; no FET payout is credited.
- Actual: `settle_match_pool` counts all active result-camp entries as winners and credits `pool_win` to every winning entry. See `supabase/migrations/20260501155600_automated_pool_settlement_engine.sql:272` and `supabase/migrations/20260501155600_automated_pool_settlement_engine.sql:577`.
- Suggested fix: Add a server-side eligibility function that checks paid, non-cancelled orders from the linked venue between `start_time - interval '2 hours'` and start time. Use it inside settlement before any payout ledger rows are created.
- Status: Fixed on remote DB, pending live UAT.

### FZ-QA-002 - Entertainment games, question selection, first-correct scoring, Music Bingo, and TV display are not backend-backed

- Severity: Blocker
- Area: Games / Screen / Realtime
- Steps: Open venue portal `/games`, `/games/new`, `/games/:sessionId/control`, `/screen`, or inspect Supabase functions/migrations for game/session/question/bingo/screen tables.
- Expected: Central game templates, venue-linked sessions, 20 stored questions, one-answer-per-team, first-correct DB protection, bingo claims, team scoring, and realtime TV state.
- Actual: Backend game/session/bingo/screen primitives now exist. Venue portal game/screen routes render, but authenticated host controls and realtime TV mode switching were not proven because the linked database has no active venue staff fixture/session for UAT.
- Suggested fix: Implement the smallest backend-backed game/TV contract before launch; do not rely on placeholder pages for UAT.
- Status: Backend fixed on remote DB and standalone TV PWA added; dashboard/TV authenticated realtime UAT remains open.

### FZ-QA-003 - Venue/bar/platform FET wallets are missing

- Severity: Critical
- Area: Wallet / Dashboard / Pools / Games
- Steps: Inspect wallet schema and venue wallet pages.
- Expected: Separate user wallet, bar/venue wallet, and platform wallet where implemented, with all movements ledgered.
- Actual: `fet_wallets` is keyed by `user_id` only. Venue wallet pages explicitly say to show a real balance only when the wallet API is connected. See `supabase/migrations/20260423050000_sports_bar_clean_baseline.sql:10420` and `apps/venue-portal/src/features/target/TargetPages.tsx:252`.
- Suggested fix: Add owner-scoped wallets or a venue-wallet table with ledgered bar top-ups, bar stakes, game rewards, and platform movements.
- Status: Fixed on remote DB for venue wallets, pending UAT; platform wallet remains outside current MVP unless explicitly launched.

### FZ-QA-004 - Pools can be created without a linked venue

- Severity: Critical
- Area: Pools / Eligibility
- Steps: Create a mobile pool with default Global scope or call `create_pool` with `p_scope='global'`.
- Expected: Every prediction pool is linked to one venue/bar; user-created pools must select a linked bar.
- Actual: `create_pool` explicitly nulls `p_venue_id` for global and country scopes. Mobile pool creation defaults to global and only requires a venue for venue scope. See `supabase/migrations/20260501132000_pool_only_gameplay_controls.sql:214` and `lib/features/pools/screens/create_pool_screen.dart:33`.
- Suggested fix: Require `p_venue_id` for all user/bar pools and use country/global fields only as browsing filters.
- Status: Fixed on remote DB in the new `create_pool`/`create_match_pool` RPCs plus mobile/website create-pool UI; pending live UAT.

### FZ-QA-005 - Pool join backend accepts locked/live pools

- Severity: Major
- Area: Pools / Wallet
- Steps: Call `join_match_pool` for a pool whose status is `locked` or `live`.
- Expected: Joining is blocked after the deadline/lock; no wallet mutation occurs.
- Actual: Backend allows `open`, `locked`, and `live`. The shared UI helper only allows `open`, so client and backend rules conflict. See `supabase/migrations/20260501160000_pool_sharing_completion.sql:370` and `packages/core/src/pools.ts:64`.
- Suggested fix: Restrict backend join to `open` and enforce joining deadline/start time in the same locked transaction used for staking.
- Status: Fixed on remote DB in the overridden `join_match_pool` RPC; pending live UAT.

### FZ-QA-006 - Pool creation lacks required stake/deadline fields

- Severity: Major
- Area: Pools / Wallet
- Steps: Inspect `create_pool` RPC and venue pool creation.
- Expected: Linked bar, match, creator stake amount, participant stake amount, joining deadline/scheduled time, and bar wallet stake where relevant.
- Actual: `create_pool` accepts min/max stake and creator reward only; inserted `entry_fee_fet` is `0`; no creator stake or deadline is reserved. See `supabase/migrations/20260501132000_pool_only_gameplay_controls.sql:121` and `supabase/migrations/20260501132000_pool_only_gameplay_controls.sql:287`.
- Suggested fix: Add creator stake and participant fixed stake contract with ledgered reservation/refund semantics.
- Status: Partially fixed on remote DB, pending UAT. Participant fixed stake now maps to `entry_fee_fet`/`stake_min`; bar official stake is reserved from the venue wallet. User creator stake reservation remains a product decision if user-created pools must stake at creation time rather than on join.

### FZ-QA-007 - Fan profile limits and editing are not implemented

- Severity: Major
- Area: Fan Profile / UI / Data
- Steps: Complete onboarding or inspect `user_favorite_teams`.
- Expected: One local team, one or two top European teams, and one or two national teams, with setup/edit UI and enforced limits.
- Actual before fix: onboarding was WhatsApp/market-focused and did not present the required fan-profile setup step. The legacy gateway could store one local team plus arbitrary popular team IDs if called, but DB source values were generic `local`, `popular`, `settings`, `synced`; no category counts or limits existed. Profile only summarized saved teams.
- Suggested fix: Add explicit category fields or category-scoped favorite rows, DB constraints/RPC validation, and edit UI.
- Status: Fixed pending live UAT. Added category helpers, onboarding setup step, profile edit sheet, gateway save method, DB source constraint update, server-side category limit trigger, and focused tests.

### FZ-QA-008 - Users can change their own 6-digit fan ID

- Severity: Critical
- Area: Auth / RLS / Profile
- Steps: Authenticated user updates own `profiles.fan_id` to any unused six-digit string.
- Expected: Fan ID is autogenerated and immutable to the user.
- Actual: Trigger regenerates only when `fan_id` is null or invalid, and the RLS policy allows users to update their own profile. See `supabase/migrations/20260423050000_sports_bar_clean_baseline.sql:2987` and `supabase/migrations/20260423050000_sports_bar_clean_baseline.sql:14897`.
- Suggested fix: On update, preserve `OLD.fan_id` unless service role/admin performs a controlled repair; revoke unnecessary execute grants on ID generators.
- Status: Fixed on remote DB in the replacement `assign_profile_fan_id` trigger function.

### FZ-QA-009 - Order enum contract mismatch could break mobile order parsing

- Severity: Major
- Area: Ordering / Flutter / Dashboard
- Steps: Return an order with `status='preparing'`, `payment_status='payment_submitted'`, or `payment_method='card'`.
- Expected: Flutter parses and displays the order.
- Actual before fix: Flutter enums did not include those values. DB/types and venue UI had moved ahead of Flutter.
- Suggested fix: Keep DB enums, shared TS types, Flutter enums, generated JSON maps, and Edge validators synchronized.
- Status: Fixed in this review

Files changed: `supabase/functions/order_update_status/index.ts`, `apps/venue-portal/src/features/orders/LiveOrderQueuePage.tsx`, `lib/models/hospitality/order_model.dart`, `lib/models/hospitality/order_model.freezed.dart`, `lib/models/hospitality/order_model.g.dart`, `lib/features/ordering/widgets/live_order_status_pill.dart`, `lib/features/ordering/screens/order_tracking_screen.dart`, `lib/features/ordering/screens/orders_screen.dart`.

### FZ-QA-010 - Order reward UI uses an estimate instead of the ledger result

- Severity: Major
- Area: Wallet / Ordering / UI
- Steps: Configure a venue reward percentage different from the hardcoded mobile estimate, then mark order paid.
- Expected: User sees actual FET earned from the ledger/order reward.
- Actual: Flutter calculates `estimatedFetEarned` as 100 FET per EUR/RWF conversion, while `credit_fet_for_order` computes from venue reward config and does not update an `orders.fet_earned` field. See `lib/models/hospitality/order_model.dart:158` and `supabase/migrations/20260501153000_remote_wallet_engine_compat.sql:397`.
- Suggested fix: Read actual order reward ledger rows or persist the credited amount on the order after reward posting.
- Status: Fixed on remote DB. The order reward RPC now persists `orders.fet_earned`, and Flutter displays `fet_earned` instead of the legacy estimate.

### FZ-QA-011 - Flutter order tracking is not wired to realtime updates

- Severity: Major
- Area: Realtime / Ordering
- Steps: Open an order tracking screen, then update order status from the venue dashboard.
- Expected: Phone updates without manual refresh/navigation.
- Actual: Gateway has `subscribeToOrder`, but tracking uses a one-shot `FutureProvider`. See `lib/features/ordering/data/order_gateway.dart:393` and `lib/features/ordering/screens/order_tracking_screen.dart:20`.
- Suggested fix: Add an auto-disposed stream/provider that subscribes to order changes and invalidates or updates the order detail state.
- Status: Fixed pending live realtime UAT. `orderRealtimeProvider` subscribes to order changes and reloads full order details.

### FZ-QA-012 - Eligibility UI is derived from payment status, not real activity eligibility

- Severity: Major
- Area: Dashboard / Settlement / UX
- Steps: Open an order detail in venue portal.
- Expected: Eligibility shows linked activity, start time, order window, paid/cancelled/order-from-linked-venue checks, and settlement state.
- Actual: Current UI marks order as eligible when payment status is paid; it does not evaluate a linked game/pool start window. See `apps/venue-portal/src/features/orders/OrderDetailPage.tsx:304`.
- Suggested fix: Back eligibility UI with a server-side eligibility view/RPC and show linked activity context.
- Status: Open

### FZ-QA-013 - Secret rotation remains a documented release blocker

- Severity: Blocker
- Area: Security / Release
- Steps: Review release docs.
- Expected: All Supabase keys/passwords/tokens rotated before production.
- Actual: Docs state Supabase credentials were shared and must be rotated. See `docs/README.md:66` and `docs/secret-rotation-runbook.md:3`.
- Suggested fix: Rotate Supabase anon key, service-role key, DB password/connection strings, personal access token, and deployment secrets before launch.
- Status: Open

### FZ-QA-014 - Test fixtures do not cover full required UAT scope

- Severity: Major
- Area: Test Data / UAT
- Steps: Inspect fixtures/tests for 3 countries, 5 bars, games, 100+ questions, 20 selected questions, eligibility winners, and simultaneous answer submissions.
- Expected: Repeatable fixtures and tests for every required UAT scenario.
- Actual: Existing unit/widget tests cover important slices, but no complete game/TV/eligibility fixture set was found.
- Suggested fix: Add deterministic Supabase seed data and backend tests for settlement eligibility, game session question selection, and first-correct answer races.
- Status: Partial. SQL contract tests now cover settlement eligibility, bar wallet staking, 20-question selection, and first-correct guards, but full seed fixtures for multi-country/multi-bar UAT still need a runnable Supabase environment.

### FZ-QA-015 - Authenticated venue dashboard UAT is blocked by missing staff fixtures

- Severity: Major
- Area: Dashboard / UAT / Test Data
- Steps: Open the linked Supabase database and inspect `public.venue_users`, then load venue portal routes.
- Expected: At least one active venue staff test account is linked to a venue so order/payment/pool/game/screen staff workflows can be executed live without production data manipulation.
- Actual: The linked database has venues but no active `venue_users` rows available for a staff session. Unauthenticated route rendering works, but staff-only data loading and RPC actions cannot be fully exercised.
- Suggested fix: Create a dedicated non-production UAT staff account, link it to a test venue, seed orders/pools/games/questions/teams, and rerun live UAT with phone app + dashboard + TV route open together.
- Status: Open

## 4. Security/RLS Findings

Positive controls:

- Orders are RLS-protected for user/venue-member select, and direct client order updates are blocked by trigger. See `supabase/migrations/20260423050000_sports_bar_clean_baseline.sql:15472` and `supabase/migrations/20260423050000_sports_bar_clean_baseline.sql:9128`.
- User wallet and ledger reads are owner-scoped; wallet mutation primitive is service-role-only after hardening. See `supabase/migrations/20260423050000_sports_bar_clean_baseline.sql:14988` and `supabase/migrations/20260501162000_wallet_rpc_grant_hardening.sql:1`.
- `join_match_pool` uses a locked pool row and wallet transaction function, which protects basic overdraft/ledger behavior.
- New venue wallet mutation is exposed only through restricted RPCs and service/venue-manager checks; direct table writes are not granted to authenticated users.
- New game answer functions validate team membership and rely on DB uniqueness for one team answer and one first-correct answer per question.
- No dangerous DOM sink matches were found for `dangerouslySetInnerHTML`, direct `innerHTML`, `eval(`, or `javascript:` in the app/source scan.

Security gaps:

- Fan ID immutability, pool creation, join rules, settlement eligibility, venue wallet permissions, game answer authorization, and first-correct race protection are deployed and covered by remote SQL/RLS tests.
- Supabase advisor ERROR findings are clear after grant/view/RLS hardening. Performance WARN findings were reduced from 232 to 224 by fixing release-touched RLS init-plan policies; remaining WARNs are legacy RLS init-plan and multiple-policy findings across unrelated modules.
- Production launch must wait for documented credential rotation.

## 5. Data Integrity Findings

- User wallet balance changes are generally ledgered through `wallet_post_transaction`, with idempotency and insufficient-balance checks.
- Venue wallet balance changes are ledgered through `venue_wallet_post_transaction`; direct balance mutation by users remains blocked by grants/RLS.
- Pool settlement now filters eligible/ineligible winners and refunds no-winner/cancelled pools in the hardening migration.
- Pool joining after lock/live is blocked by the overridden `join_match_pool`.
- Game answer duplicates and first-correct races are protected with DB constraints/indexes.
- Order earned FET displayed in Flutter now uses `orders.fet_earned`; the remote reward RPC updates that value from the ledgered reward mutation.

## 6. UX Findings

- Mobile auth/onboarding is simple and name-free. Fan profile setup/editing now exposes local, top-European, and national categories with visible selection counts and empty states.
- Payment confirmation UX in venue portal is now stronger: manual mark-paid uses a modal with staff confirmation, amount, method, reference, and note.
- Pool creation UI now requires a linked venue/bar instead of a global venue-less pool.
- Pool join/detail screens still need clearer qualifying-order copy before staking.
- Dashboard games/screen/wallet pages render in browser screenshots, but authenticated staff actions still need UAT with seeded venue staff.
- TV state now exists server-side, `/screen` renders a desktop/mobile control preview, and `apps/tv-display` renders the paired live display surface; realtime switching still needs authenticated UAT.
- Eligibility badges exist but still need a linked-activity eligibility view/RPC for operational clarity.

## 7. Realtime Findings

Working or partially working:

- Venue portal order queue subscribes to `orders`.
- Venue stats subscribe to `orders` and `match_pools`.
- Venue pool list subscribes to `match_pools`.

Missing or broken:

- Flutter order tracking now uses a realtime provider; live phone/dashboard UAT is still required.
- Pool participant join/settlement realtime was not proven end-to-end.
- Game player joins, team creation, answer submission, first-correct reveal, leaderboard, settlement, and TV display now have backend state primitives but realtime subscriptions/display behavior still need end-to-end validation with test fixtures.
- Offline/reconnect behavior was not validated.

## 8. Test Matrix

| Test case / check | Status | Notes |
| --- | --- | --- |
| Static repo inspection | Pass | Flutter, web apps, venue portal, admin, Supabase migrations/functions reviewed. |
| `flutter analyze` | Pass | No issues after targeted fix. |
| `flutter test` | Pass | 221 Flutter tests passed in the current worktree, including order reward and fan-profile category tests. The modified golden file in git status still needs review before commit. |
| `npm run typecheck` | Pending rerun | Admin, venue portal, website, TV display, and core typecheck must pass after the TV package addition. |
| `npm test` | Pass | Admin 22 tests passed; website 6 tests passed. |
| `deno check supabase/functions/order_update_status/index.ts` | Pass | Edge Function compiles after status fix. |
| Targeted Flutter auth-flow test | Pass | OTP row overflow fixed; targeted integration test passes. |
| Remote Supabase migration push | Pass | Migrations through `20260503140000` are applied to the linked FANZONE project. |
| Remote SQL contract tests | Pass | `release_readiness_hardening.sql` executed successfully through `supabase db query --linked`. |
| Remote RLS audit tests | Pass | `rls_hardening_audit.sql` executed successfully through `supabase db query --linked`. |
| Supabase advisor ERROR checks | Pass | Linked security and performance advisors report no ERROR-level issues after hardening. Performance WARN count is now 224 after targeted release-policy cleanup. |
| Live venue route screenshots | Partial | Captured `output/playwright/uat-venue-overview.png`, `uat-venue-orders.png`, `uat-venue-pools.png`, `uat-venue-games.png`, `uat-venue-wallet.png`, `uat-venue-screen.png`, and `uat-venue-screen-mobile.png`. Routes return HTTP 200, but staff actions are blocked by missing venue staff fixtures. |
| Live mobile onboarding screenshot | Partial | Captured `output/playwright/uat-website-onboarding-mobile.png`; Flutter web-server did not produce a URL in this environment, so Flutter live browser UAT remains blocked. |
| Local Supabase DB lint/tests | Blocked | Local Supabase/Docker remains unavailable (`127.0.0.1:54322` refused; Colima socket unavailable). Remote SQL checks were used instead. |
| Deno Edge unit tests | Not rerun in final pass | Earlier review pass compiled the order status function; current hardening is SQL-only for Supabase backend. |
| Scenario A: New user onboarding | Partial | OTP/no-name path covered by code/tests; fan profile setup/editing is implemented and tested, but live OTP UAT was not executed. |
| Scenario B: Order and eligibility | Pending live UAT | Settlement backend now has eligibility logic; live dashboard/app update path not validated. |
| Scenario C: User joins pool without order | Fixed pending live UAT | Settlement now marks ineligible winners with zero payout. |
| Scenario D: User joins pool with qualifying order | Fixed pending live UAT | Settlement now pays only eligible winners and ledgers payouts. |
| Scenario E: Bar creates prediction pool | Fixed pending live UAT | Venue pool creation supports bar stake reservation through the venue wallet. |
| Scenario F: Insufficient FET | Fixed pending live UAT | User and venue wallet debits check available balances before ledger mutation. |
| Scenario G: Bar starts Fan Trivia | Backend fixed pending UI/UAT | Backend can create/start a venue session and store exactly 20 questions. |
| Scenario H: First correct team rule | Backend fixed pending live UAT | Backend enforces first-correct uniqueness. |
| Scenario I: Music Bingo | Backend fixed pending UI/UAT | Backend supports claim submission and venue verification without streaming. |
| Scenario J: TV screen | Partial | Venue screen state exists, `/screen` desktop/mobile screenshots render, and `apps/tv-display` exists; paired TV/realtime switching still needs authenticated UAT. |
| Scenario K: Venue order operations | Partial | Manual payment and service operations exist; full live UAT not run. |
| Scenario L: Settlement audit | Fixed pending live UAT | Migration implements eligible-only payouts, ineligible winner separation, and idempotent settlement records. |

## 9. Required Fixes Before Launch

Blockers:

- Rotate all documented Supabase credentials before production.
- Seed dedicated UAT users/venue staff/game/order fixtures and run authenticated live UAT for app + venue dashboard + TV against the deployed remote backend.

Critical/must-fix:

- Complete authenticated venue dashboard/TV game workflows and verify `apps/tv-display` against the new game, wallet, and screen RPCs.

## 10. Nice-to-Have Improvements

- Add pagination/infinite loading for venue orders beyond the current 24-hour window and global pool listings beyond 50 rows.
- Add reconciliation dashboards for user/venue/platform wallets.
- Add explicit mobile copy for the 2-hour eligibility rule on pool detail/join screens.
- Add authenticated Playwright/browser UAT for venue dashboard and TV after UAT staff fixtures are seeded.

## 11. Final UAT Signoff Checklist

| Surface | Signoff | Reason |
| --- | --- | --- |
| User app | No | Fan profile rules are implemented, but live WhatsApp OTP, order eligibility, realtime order tracking, and settlement behavior still need live UAT. |
| Venue dashboard | No | Routes render and core ordering code exists, but authenticated staff workflows need test fixtures and live RPC/realtime UAT. |
| TV screen | Partial | Server-side state, screen control preview, and standalone TV display app exist; paired realtime behavior is not signed off. |
| Supabase backend | Partial | Remote migrations and SQL/RLS/advisor ERROR checks pass; live UAT remains. |
| FET ledger | Partial | User and venue ledger paths are implemented and contract-tested; production data reconciliation/UAT remains. |
| Settlement | Partial | Eligibility settlement is implemented and SQL-tested; live UAT remains. |
| Security | No | Credential rotation is still a launch blocker after secrets were exposed in chat. |
| Performance | Partial | No advisor ERROR findings; 224 performance WARN findings and full load/realtime/game/TV performance remain unvalidated. |

## Alignment Notes

The targeted fixes preserved the current architecture:

- It reused the existing Edge Function for order status transitions.
- It reused the existing venue dashboard order queue and manual payment modal pattern.
- It reused `user_favorite_teams` for fan profile categories instead of adding a duplicate profile table.
- It updated Flutter enums and generated JSON maps rather than adding duplicate parsing logic.
- It extended existing Supabase RPC/migration patterns instead of adding a separate backend service.
- It kept payments off-platform and preserved staff manual confirmation/audit behavior.
- It uses venue-linked pools and global/country browsing filters without creating global competition logic.
- It did not introduce payment API integration, fake game data, duplicate client-side wallet mutation, or unrelated redesigns.
