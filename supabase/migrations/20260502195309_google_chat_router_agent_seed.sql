-- Remote history marker.
--
-- This migration version exists on the linked Supabase project as
-- "google_chat_router_agent_seed". The authoritative SQL was applied
-- remotely before this local checkout contained the migration file.
--
-- Keep this no-op marker so future `supabase db push` checks do not fail
-- because the remote migration history contains a version absent locally.

